diff --git a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
index e6f8fef21367b8ed924a23d67c3b9752e7105cbc..3fd585ad8cd8c50a3b9eec27a9408890baee7b17 100644
--- a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
+++ b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
@@ -32,6 +32,7 @@
 #include "base/byte_size.h"
 #include "base/compiler_specific.h"
 #include "base/feature_list.h"
+#include "base/command_line.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
@@ -70,6 +71,7 @@
 #include "third_party/blink/renderer/core/html/canvas/html_canvas_element.h"
 #include "third_party/blink/renderer/core/html/canvas/image_data.h"
 #include "third_party/blink/renderer/core/html/canvas/predefined_color_space.h"
+#include "third_party/blink/renderer/core/kroma_canvas/canvas_interventions_helper.h"
 #include "third_party/blink/renderer/core/html/html_image_element.h"
 #include "third_party/blink/renderer/core/html/media/html_video_element.h"
 #include "third_party/blink/renderer/core/imagebitmap/image_bitmap.h"
@@ -637,6 +639,16 @@ static String ExtractWebGLContextCreationError(
   return builder.ToString();
 }
 
+static String GetStringWithOptionalCommandLineOverride(
+    const char* switch_name,
+    const GLubyte* original_value) {
+  const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
+  if (command_line->HasSwitch(switch_name)) {
+    return String(command_line->GetSwitchValueASCII(switch_name).c_str());
+  }
+  return String(reinterpret_cast<const char*>(original_value));
+}
+
 std::unique_ptr<WebGraphicsContext3DProvider>
 WebGLRenderingContextBase::CreateContextProviderInternal(
     CanvasRenderingContextHost* host,
@@ -1856,7 +1868,12 @@ WebGLRenderingContextBase::PaintRenderingResultsToSnapshot(
   if (Host()->LowLatencyEnabled() &&
       GetDrawingBuffer()->SupportsNoCopyExportForLowLatency()) {
     auto resource = ExportLowLatencyCanvasResource(source_buffer);
-    return resource ? resource->Bitmap() : nullptr;
+    scoped_refptr<StaticBitmapImage> snapshot = resource ? resource->Bitmap()
+                                                         : nullptr;
+    if (snapshot) {
+      CanvasInterventionsHelper::MaybeNoiseSnapshot(snapshot);
+    }
+    return snapshot;
   }
 
   bool cleared_content = ClearIfComposited(kClearCallerOther) != kSkipped;
@@ -1879,7 +1896,11 @@ WebGLRenderingContextBase::PaintRenderingResultsToSnapshot(
   if (!must_paint_to_canvas_ && !cleared_content) {
     if (resource_provider_.get()) {
       // `resource_provider_` already has the current contents.
-      return resource_provider_->Snapshot();
+      scoped_refptr<StaticBitmapImage> snapshot = resource_provider_->Snapshot();
+      if (snapshot) {
+        CanvasInterventionsHelper::MaybeNoiseSnapshot(snapshot);
+      }
+      return snapshot;
     }
     if (cached_snapshot_) {
       // `cached_snapshot__` already has the current contents.
@@ -1900,6 +1921,9 @@ WebGLRenderingContextBase::PaintRenderingResultsToSnapshot(
       cached_snapshot_ = CopyRenderingResultsToUnacceleratedStaticBitmapImage(
           source_buffer, viz::SharedImageFormat::N32Format(),
           kPremul_SkAlphaType, kTopLeft_GrSurfaceOrigin);
+      if (cached_snapshot_) {
+        CanvasInterventionsHelper::MaybeNoiseSnapshot(cached_snapshot_);
+      }
     }
 
     if (cached_snapshot_) {
@@ -1932,7 +1956,11 @@ WebGLRenderingContextBase::PaintRenderingResultsToSnapshot(
 
   // We successfully painted the canvas' contents.
   must_paint_to_canvas_ = false;
-  return resource_provider->Snapshot();
+  scoped_refptr<StaticBitmapImage> snapshot = resource_provider->Snapshot();
+  if (snapshot) {
+    CanvasInterventionsHelper::MaybeNoiseSnapshot(snapshot);
+  }
+  return snapshot;
 }
 
 scoped_refptr<CanvasResource>
@@ -3881,7 +3909,10 @@ ScriptValue WebGLRenderingContextBase::getParameter(ScriptState* script_state,
     case GL_RENDERBUFFER_BINDING:
       return WebGLAny(script_state, renderbuffer_binding_.Get());
     case GL_RENDERER:
-      return WebGLAny(script_state, String("WebKit WebGL"));
+      return WebGLAny(
+          script_state,
+          GetStringWithOptionalCommandLineOverride(
+              "webgl-renderer", ContextGL()->GetString(GL_RENDERER)));
     case GL_SAMPLE_ALPHA_TO_COVERAGE:
       return GetBooleanParameter(script_state, pname);
     case GL_SAMPLE_BUFFERS:
@@ -3959,7 +3990,10 @@ ScriptValue WebGLRenderingContextBase::getParameter(ScriptState* script_state,
     case GC3D_UNPACK_COLORSPACE_CONVERSION_WEBGL:
       return WebGLAny(script_state, unpack_colorspace_conversion_);
     case GL_VENDOR:
-      return WebGLAny(script_state, String("WebKit"));
+      return WebGLAny(
+          script_state,
+          GetStringWithOptionalCommandLineOverride(
+              "webgl-vendor", ContextGL()->GetString(GL_VENDOR)));
     case GL_VERSION:
       return WebGLAny(
           script_state,
@@ -3977,8 +4011,10 @@ ScriptValue WebGLRenderingContextBase::getParameter(ScriptState* script_state,
       return ScriptValue::CreateNull(script_state->GetIsolate());
     case WebGLDebugRendererInfo::kUnmaskedRendererWebgl:
       if (ExtensionEnabled(kWebGLDebugRendererInfoName)) {
-        return WebGLAny(script_state,
-                        String(ContextGL()->GetString(GL_RENDERER)));
+        return WebGLAny(
+            script_state,
+            GetStringWithOptionalCommandLineOverride(
+                "webgl-renderer", ContextGL()->GetString(GL_RENDERER)));
       }
       SynthesizeGLError(
           GL_INVALID_ENUM, "getParameter",
@@ -3986,8 +4022,10 @@ ScriptValue WebGLRenderingContextBase::getParameter(ScriptState* script_state,
       return ScriptValue::CreateNull(script_state->GetIsolate());
     case WebGLDebugRendererInfo::kUnmaskedVendorWebgl:
       if (ExtensionEnabled(kWebGLDebugRendererInfoName)) {
-        return WebGLAny(script_state,
-                        String(ContextGL()->GetString(GL_VENDOR)));
+        return WebGLAny(
+            script_state,
+            GetStringWithOptionalCommandLineOverride(
+                "webgl-vendor", ContextGL()->GetString(GL_VENDOR)));
       }
       SynthesizeGLError(
           GL_INVALID_ENUM, "getParameter",
@@ -5133,6 +5171,12 @@ void WebGLRenderingContextBase::ReadPixelsHelper(GLint x,
     }
     ContextGL()->ReadPixels(x, y, width, height, format, type, data);
   }
+
+  // Apply noise to prevent fingerprinting via readPixels
+  if (format == GL_RGBA && type == GL_UNSIGNED_BYTE && data && width > 0 &&
+      height > 0) {
+    CanvasInterventionsHelper::MaybeNoiseReadPixels(data, width, height);
+  }
 }
 
 void WebGLRenderingContextBase::RenderbufferStorageImpl(
