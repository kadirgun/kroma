diff --git a/third_party/blink/renderer/core/html/canvas/canvas_rendering_context.h b/third_party/blink/renderer/core/html/canvas/canvas_rendering_context.h
index af36d47dbb5ace4edd3fee0a6cb08217ba85a1de..1ca4547d6c07f7b0617c977e8b0cd914497bf351 100644
--- a/third_party/blink/renderer/core/html/canvas/canvas_rendering_context.h
+++ b/third_party/blink/renderer/core/html/canvas/canvas_rendering_context.h
@@ -171,6 +171,10 @@ class CORE_EXPORT CanvasRenderingContext
 
   virtual scoped_refptr<StaticBitmapImage> GetImage() = 0;
   virtual bool IsComposited() const = 0;
+  // Returns true if this context has recorded canvas operations.
+  // Used by Kroma's fingerprinting protection to determine whether to apply
+  // noise to canvas snapshots (toDataURL, toBlob, getImageData).
+  virtual bool HasOperations() const { return false; }
 
   // Called when the entire tab is backgrounded or unbackgrounded.
   // The page's visibility status can be queried at any time via
