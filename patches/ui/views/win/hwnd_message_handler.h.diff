diff --git a/ui/views/win/hwnd_message_handler.h b/ui/views/win/hwnd_message_handler.h
index deba19101c4e7c5a9396b40ac86c90584877fc08..1ceead0c4ca712574079a3a19e395ed55e3dddb4 100644
--- a/ui/views/win/hwnd_message_handler.h
+++ b/ui/views/win/hwnd_message_handler.h
@@ -838,6 +838,10 @@ class VIEWS_EXPORT HWNDMessageHandler : public gfx::WindowImpl,
   // fullscreen window which lost activation. Defaults to false.
   bool background_fullscreen_hack_;
 
+  // Set to true once the window has actually become visible on screen.
+  // Used to ensure the window opens inactive on its first real show.
+  int has_become_visible_ = 0;
+
   // True if the window should have no border and its contents should be
   // partially or fully transparent.
   bool is_translucent_ = false;
